# Codex outstanding-started pack dedupe

This is a placeholder until the Codex outstanding-started pack lands. Bot Commander must then:

1. Compare the pack IDs with the IDs in `tracker/items.json`.
2. Mark duplicate cards rather than counting them twice.
3. Update the live banner and `itemsNote` with the deduped count and timestamp.

Until that comparison is complete, the 270 imported Platform rows are a live mirror count, not a final deduped programme count.
